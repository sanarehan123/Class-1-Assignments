function make_shirt(size: string, message: string): void {
    console.log(`The T-shirt is size ${size} and it has the message: "${message}".`);
  }
  
  // Call the function
  make_shirt('XL', 'Hello, Earth!');

